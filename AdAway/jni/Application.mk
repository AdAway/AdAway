# Optimizations
APP_OPTIM := release

# Build target
APP_ABI := armeabi armeabi-v7a x86 x86_64 mips mips64

# If APP_MODULES is not set, all modules are compiled!
APP_MODULES := blank_webserver tcpdump
