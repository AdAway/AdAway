一款使用hosts文件的广告拦截器。
hosts文件是一个系统文件，其中包含主机名和IP地址之间的映射列表。
当一个应用向该文件中的主机请求广告时，请求会被重定向至本地IP 127.0.0.1，从而失效。
在应用中你可以下载预定义的hosts文件，将它们合并进系统的hosts文件。你也可以使用自己的文件，将特定主机添加到白名单/黑名单中。
对于被拦截的主机名，可以选择运行一个本地的网络服务器来响应请求，或者将请求定向到指定的IP地址。

需要root权限：√。hosts文件位于/system，默认为只读。