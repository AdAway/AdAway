一款使用 hosts 文件的广告拦截器。
hosts 文件是一个系统文件，其中包含主机名和 IP 地址之间的映射表。
当一个应用向该文件中的主机请求广告时，请求会被重定向至本地 IP 127.0.0.1，从而失去效果。
在本应用中，你可以下载预定义的 hosts 文件，将它们合并入系统的 hosts 文件。你也可以使用自己的文件，将特定的主机添加到白名单与黑名单中。
可以选择运行一个本地的网络服务器来响应对被拦截的主机名的请求，也可以请求重定向到指定的 IP 地址。

需要 root 权限：√。hosts 文件位于 /system 目录下，默认为只读。